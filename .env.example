# WebMCP server base URL
# Leave empty to use built-in mock tools for demo
VITE_WEBMCP_BASE_URL=

# Claude proxy URL
# Anthropic API blocks direct browser requests due to CORS.
# Point this to a lightweight proxy (e.g. nginx, Express) that forwards to api.anthropic.com
VITE_CLAUDE_PROXY_URL=
